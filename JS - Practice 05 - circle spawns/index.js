( () => {

    const canvas = document.querySelector('canvas');
    const ctx    = canvas.getContext('2d');

    let w, h;

    class Dot {
        constructor() {
            this.pos    = { x: , y: }
        }
    }

    function init() {
        w
        h
    }

})()